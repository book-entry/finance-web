# Committed dev defaults. Override locally in .env.development.local
# (gitignored) — for example to point at a deployed gateway:
#
#   echo 'VITE_GATEWAY_URL=https://api.your-server.com' > .env.development.local
#
# See .env.example for all available variables.
VITE_GATEWAY_URL=https://book-entry-api.ansonwonggg.com